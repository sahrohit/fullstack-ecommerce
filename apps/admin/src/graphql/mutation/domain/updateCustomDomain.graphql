mutation UpdateCustomDomain($customDomain: String!) {
	updateCustomDomain(customDomain: $customDomain) {
		id
		name
		desc
		address
		logo
		font
		subdomain
		customDomain
		defaultForPreview
		userId
		categoryId
		created_at
		updated_at
	}
}
