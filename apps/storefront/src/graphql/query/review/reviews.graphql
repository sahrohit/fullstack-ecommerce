query Reviews($productId: Int!) {
	reviews(productId: $productId) {
		id
		productId
		userId
		rating
		desc
		review
		isAnonymous
		created_at
		updated_at
		user {
			first_name
			last_name
		}
	}
}
