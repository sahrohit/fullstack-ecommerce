mutation UpdateCategory($options: UpdateCategoryInput!, $id: Int!) {
	updateCategory(options: $options, id: $id) {
		id
		name
		identifier
		desc
		imageURL
		tenantId
		created_at
		updated_at
	}
}
