query MeWithAccount {
	meWithAccount {
		...UserFragment
		accounts {
			id
			userId
			type
			provider
			providerAccountId
			refresh_token
			expires_at
			token_type
			scope
			id_token
			session_state
			created_at
			updated_at
			access_token
		}
	}
}
